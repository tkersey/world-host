# Conformance

The executable proof is `test/research_lookup_fixture.test.mjs`. The checked
corpus and `conformance-receipt.json` bind its vectors to the exact pack
fingerprint and World release identities.
