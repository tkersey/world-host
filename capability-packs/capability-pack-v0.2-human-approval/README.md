# Human Approval Capability Pack v0.2

Reference pack for host-local human approval. It supports interactive terminal policy in host code and noninteractive allow/deny fixtures for tests.

The manifest selects the receiver-owned `human-approval` driver; the pack carries no executable adapter code.

Approval records are host claims only. They are not World receipts or cryptographic attestations.
